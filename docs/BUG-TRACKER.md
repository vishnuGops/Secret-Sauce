# BUG-TRACKER — Secret-Sauce

Tracks all bugs found or fixed. Update in the same change that discovers/fixes a bug
(see "Docs–code sync" in [CLAUDE.md](../CLAUDE.md)).

Severity: `blocker` \| `high` \| `medium` \| `low`
Status: `open` \| `in-progress` \| `fixed` \| `wontfix`

| ID   | Date       | Severity | Area          | Description                                                                  | Status | Fix / Commit                                                                      |
| ---- | ---------- | -------- | ------------- | ---------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------- | --- | ---- | ---------- | ---- | ------------- | --------------------------------------------------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------- |
| B001 | 2026-07-28 | low      | design_system | `RecipeCard` `RenderFlex` overflow when given unbounded height (test only).  | fixed  | Test constrains card to a 320px-wide box, matching real grid usage.               |
| B002 | 2026-07-28 | low      | home          | Feature-card grid overflowed inside the 900px content column (fixed aspect). | fixed  | Switched grid to `mainAxisExtent: 132` and clamped card text (maxLines/ellipsis). |
| B003 | 2026-07-28 | medium   | recipe_editor | New/Edit recipe screen had no back or cancel affordance (reached via `go`).  | fixed  | Added a Cancel (×) `leading` button with a discard-confirmation dialog.           |     | B004 | 2026-07-28 | high | android build | Release APK build failed: `path_provider_android` 2.3.x pulls a JNI/CMake native build; Android CMake download was blocked. | fixed | Pinned `path_provider_android` to `>=2.2.0 <2.3.0` (pre-JNI) via `pubspec_overrides.yaml`; added `INTERNET` permission. |

### Toolchain / setup bugs (found while re-provisioning the dev environment, 2026-08-18)

| ID   | Date       | Severity | Area     | Description                                                                                                                                                                  | Status  | Fix / Commit                                                                                                                          |
| ---- | ---------- | -------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| B005 | 2026-08-18 | blocker  | codegen  | `melos run build_runner` crashes on Flutter 3.47.0 / Dart 3.13.0: `Exception: Missing implementation of visitDotShorthandPropertyAccess`. `freezed` 2.x → `source_gen` 2.x caps `analyzer` at 7.x, which only understands language 3.9. Hits `freezed` **and** `riverpod_generator`. | fixed   | Pinned the SDK to Flutter 3.44.8 / Dart 3.12.2. Permanent fix is migrating to `freezed` 3.x (breaking model syntax) — not done.        |
| B006 | 2026-08-18 | high     | tooling  | Any `melos run <script>` whose script declares `packageFilters` (`test`, `build_runner`, `build:*`, `gen:icons`) prompts "Select a package to run …". With no TTY it aborts with `StdinException: Error getting terminal echo mode`, so CI/scripted runs fail. Not version-specific — melos 6.3.3 and 8.3.0 both do it. `analyze`/`format` declare no filters and are unaffected. | fixed   | Pass `--no-select`. Added to `README.md`, `CLAUDE.md`, and `.github/workflows/ci.yml`. Compounded by B007's silent exit-0.            |
| B007 | 2026-08-18 | medium   | tooling  | `melos.bat` exits 0 even when melos itself crashed — seen both with `Can't load Kernel binary: Invalid kernel binary format version` (snapshot built against a different Dart, after an SDK switch) and with the B006 prompt abort. Scripted runs therefore report success on failure. | fixed   | Re-run `dart pub global activate melos 6.3.3` after any Flutter/Dart change. Do not trust melos exit codes in scripts — grep output for `SUCCESS`/`FAILED`. |
| B008 | 2026-08-18 | medium   | build    | `GridView.count(mainAxisExtent:)` (B002's fix) does not exist on Flutter ≤ 3.35.x, so the app fails to compile there: `The named parameter 'mainAxisExtent' isn't defined`.   | wontfix | Not an issue on the pinned 3.44.8. If an older SDK is ever needed, use `GridView.builder` + `SliverGridDelegateWithFixedCrossAxisCount`, which has had `mainAxisExtent` since Flutter 2.0. |
| B009 | 2026-08-18 | medium   | tooling  | `pubspec.lock` is git-ignored (`.gitignore:7`), so dependency resolution is not reproducible — the same commit resolves different `analyzer`/`freezed` versions on different machines and dates. This is the root cause behind B005 appearing "suddenly". | fixed  | **OPT-T4.** `pubspec.lock` removed from `.gitignore`; all four lockfiles (root, core, design_system, app) committed. `melos bootstrap` resolves against them, so the same commit resolves the same versions on every machine and a dependency change arrives as a reviewable diff instead of a silent re-resolution. |
| B010 | 2026-08-18 | high     | security | `.gitignore` matched only `**/env.local.json`. A credentials file saved as `apps/app/env.local` (no extension) held live `SUPABASE_URL` / `SUPABASE_ANON_KEY` but was **not** ignored, so it was one `git add -A` away from being committed. It also silently did nothing, since `--dart-define-from-file` reads `env.local.json`. | fixed   | Broadened to extension-less globs `env.local*` / `**/env.local*` / `**/env.*.local*`, with `!**/env.example.json` to keep the template tracked. Verified the file was never staged or committed. |

### Schema / RLS bugs (found while adding ratings, 2026-08-18)

| ID   | Date       | Severity | Area   | Description                                                                                                                                                                                                                                                                                          | Status | Fix / Commit                                                                                                                                                                                     |
| ---- | ---------- | -------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B011 | 2026-08-18 | high     | schema | Counter triggers ran with **invoker** rights, but `recipes_update` (RLS) only allows `owner_id = auth.uid()`. So liking/saving another user's recipe updated **0 rows** — silently, with no error — and `like_count` / `save_count` stayed at 0 for everyone except the owner. The new rating aggregate would have had the same hole. | fixed  | `on_like_change`, `on_save_change`, `on_rating_change` are now `security definer set search_path = public`. `bump_count` / `recompute_recipe_rating` stay invoker-rights with EXECUTE revoked from `public`, `anon`, `authenticated` so they are not callable as PostgREST RPCs. |
| B013 | 2026-08-18 | blocker  | schema | The schema granted nothing to the PostgREST roles, relying on Supabase's legacy "grant all on new tables to anon/authenticated" defaults. Current Supabase images only default to `Dxt` (TRUNCATE/REFERENCES/TRIGGER), so on a freshly provisioned project (and on `supabase db reset` locally) **every** API call failed with `permission denied for table recipes`. The existing hosted project works only because it predates that change. | fixed  | Added an explicit grants block to `0001_init.sql`: `usage` on schema `public` + `select` to `anon`/`authenticated`, DML to `authenticated`, `insert on recipe_views` to `anon`. Guarded on the `anon` role existing so plain Postgres still applies. RLS still filters rows. |
| B014 | 2026-08-18 | high     | seed   | `seed_recipe()` returned early when a recipe with that title already existed, so re-running `seed.sql` on an **already-seeded** database skipped the new rating inserts entirely. Symptom: schema applied fine, `rating_avg`/`rating_count` stayed `0` for every curated recipe, and the UI correctly rendered "no ratings" — looking like the feature was missing. | fixed  | Rating insertion moved into `seed_ratings(recipe, ratings)`; the early-return path now calls it before returning, so a re-run backfills ratings without touching existing content. Verified by deleting all `recipe_ratings` locally and re-running the seed. |
| B015 | 2026-08-18 | high     | schema | A signed-in user with an `auth.users` row but **no `profiles` row** could not rate, save, or even open a recipe: every FK into `profiles` failed with `23503 … Key is not present in table "profiles"` (seen on `recipe_ratings_user_id_fkey` and `recipe_views_user_id_fkey`). Happens to accounts created before the `profiles` table/trigger existed, and to every account after a `db:drop` — that drops `profiles` while `auth.users` survives, and `on_auth_user_created` only fires on **insert**, so the row is never rebuilt. | fixed  | `0001_init.sql` now backfills `profiles` from `auth.users` for any missing id (idempotent, runs on every apply), and `handle_new_user()` inserts `on conflict (id) do nothing` so a stale profile can't fail a signup. Reproduced locally with a profile-less user, then verified view + rating both succeed after the backfill. |
| B012 | 2026-08-18 | medium   | schema | `recipes.view_count` was never incremented — `logView()` inserts into `recipe_views`, but no trigger rolled that up. `recipes_trending` scores on `like_count + view_count`, so trending was effectively likes-only (seeded recipes looked fine because the seed writes `view_count` directly). Fixing it naively would have been worse: `recipe_views` has no unique constraint and `anon` holds `insert` on it, so a `+1 per row` trigger would have let an unauthenticated refresh loop inflate trending without limit. A second hole surfaced on the way: `views_insert`'s `with check` verified only `can_read_recipe(recipe_id)`, never `user_id`, so any client could attribute views to another user. | fixed  | Added `on_view_insert()` (`security definer set search_path = public`, per B011) counting **distinct signed-in viewers**: it skips null-`user_id` rows and any row where that user already has one for the recipe, backed by `recipe_views_recipe_user_idx`. No unique constraint on purpose — PostgREST cannot express `on conflict` inference against a partial index, so a duplicate would surface to the client as `23505`; deduping in the trigger keeps `logView()` a plain insert and keeps the full log. The probe is a read-then-write, so it takes a per-(recipe, user) `pg_advisory_xact_lock` — without it two concurrent first-views from one account each miss the other's uncommitted row and both bump (reproduced: 940 → 942). `views_insert` tightened to `user_id is null or user_id = auth.uid()`. `on_view_insert` added to `drop.sql`; the now-redundant `recipe_views_recipe_idx` dropped. Note `view_count` is **monotonic** — `user_id` is `on delete set null` and nothing decrements, so it is an upper bound on distinct viewers, not an exact count. Verified on the local stack — see the 2026-08-18 view-count run below. |

### UI bugs (found reviewing the ratings change, 2026-08-18)

| ID   | Date       | Severity | Area          | Description                                                                                                                                                                                                                                                                            | Status | Fix / Commit                                                                                                                                                                                                                          |
| ---- | ---------- | -------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B016 | 2026-08-18 | medium   | design_system | `RecipeCard`'s metadata row had no flexible child, so adding `RatingPill` overflowed it (`RenderFlex overflowed by 73 pixels`) on a rated recipe with a long time label — e.g. "12h 45m" + "4.5 (1250)" at the 276px 2-column card width. Same class as B001/B002. Worst at large text scale. | fixed  | Metadata group wrapped in `Expanded`; time label, `RatingPill` (both texts), and `DifficultyBadge` label now `Flexible` + ellipsis. Regression tests cover 276/320px at 1.0x and 2.0x text scale.                                       |
| B018 | 2026-08-18 | high     | seed / security | `seed.sql` created the 8 "taster" accounts and the "Secret Sauce Kitchen" account with **literal passwords written into the file** (`crypt('<literal>', gen_salt('bf'))`, redacted here — the kitchen one is recoverable from history before this fix) and `email_confirmed_at` pre-set. `README.md` documents pasting `seed.sql` into the **hosted** dashboard SQL Editor, so following the project's own instructions created 9 confirmed, log-in-able production accounts whose credentials were committed to the repo. `authenticated`-tier access: rate recipes, create spam recipes, like/save. `drop.sql` spares `auth.users`, so `db:reset` could not remove them. | fixed  | Both now use `crypt(gen_random_uuid()::text, gen_salt('bf'))` — nothing ever signs in as them; they exist only because `profiles.id` FKs `auth.users`. Verified on the local stack: seed applies, 6 recipes / 41 ratings / 9 users, both old passwords authenticate 0 rows, re-run still idempotent. **Any already-seeded database keeps the old hashes** (`on conflict (id) do nothing`) — rotate or delete those 9 accounts manually. **OPT-S8:** `supabase/scripts/rotate_seed_passwords.sql` now does it — re-`crypt`s all 16 seeded accounts (8 tasters + kitchen + 7 chefs) to `gen_random_uuid()`, targeted by the seed's **fixed UUIDs** rather than an email/id pattern (checklist §8 teardown-scope rule). It **rotates, never deletes**: `profiles.id` → `auth.users` and `recipes.owner_id` → `profiles` both cascade, so deleting `…aa` would take the Kitchen's 14 curated recipes and deleting the tasters would take the demo ratings. Verified on the local stack — 16/16 rotated, hash changed, and 14 kitchen recipes / 1,903 ratings / 1,016 profiles all still present. Idempotent. **Still open** because the fix must be applied to the *hosted* project, which writes to production `auth.users` — deliberately left as the owner's action rather than automated from a dev session. |
| B017 | 2026-08-18 | medium   | design_system | `StarRatingInput` kept a stale preview after a cancelled gesture: press a star, hold past the tap deadline, then scroll — the enclosing `ListView` claims the gesture, `onTapUp`/`onHorizontalDragEnd` never fire, so `onChangeEnd` never fires but `_preview` stays set. The stars then show an unsaved rating that `didUpdateWidget` cannot clear, because `value` never changed. | fixed  | Added `onTapCancel` / `onHorizontalDragCancel` → `_cancel()`, which drops `_preview` and re-emits `value`. Regression test drives press-hold-scroll inside a `ListView`.                                                                 |

### Data-ordering bugs (found planning the chefs/leaderboard feature, 2026-08-18)

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B022 | 2026-08-18 | high     | core | Recipe steps (and ingredients, and both group lists) render in **reverse order** — the last step shows first. Root cause: postgrest-dart's `.order(column)` defaults to `ascending: false`, and the four nested-content fetches in `SupabaseRecipeRepository._fetchIngredientGroups` / `_fetchStepGroups` ([recipe_repository.dart:273-315](../packages/core/lib/src/repositories/recipe_repository.dart)) omit `ascending: true` — so `ingredient_groups`, `ingredients`, `step_groups`, and `steps` all come back descending. Every other `.order()` in the repo passes `ascending: false` explicitly and is correct. **Secondary damage:** `update()` round-trips through `getById()` — the editor loads the reversed list, and `_persistContent` re-indexes it `0..n` on save, so the **stored** order flips on every edit. A recipe edited an odd number of times has reversed rows in the DB (and then *displays* correctly under the current bug — double reversal). `_appendVersion` snapshots via `getById()` too, so `content_snapshot` list order is also unreliable for edited recipes. Seeded recipes are unaffected in storage (`seed.sql` writes ascending SQL directly and they are never edited). | fixed | `ascending: true` added to all four call sites in `_fetchIngredientGroups` / `_fetchStepGroups`, with a comment naming the postgrest-dart default so it is not "cleaned up" later. Verified on the local stack in both directions — see the B022 verification run below. **Stored-order audit: nothing to repair.** All 6 recipes on the hosted project have exactly one `recipe_versions` row (`version_number = 1`, `'Seeded recipe'`), so no recipe has ever been saved through the editor and no persisted order was ever flipped. (`updated_at` on those rows has moved, but that is `recipes_touch` firing for the rating/counter triggers, not an edit.) **Residual gap:** the project's one real account (`Vishnu`, created 2026-08-18) may own private recipes, which are not readable with the anon key — if any were created *and* edited before this fix, check them by hand. |

### UI bugs (found building the chefs leaderboard, 2026-08-18)

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B023 | 2026-08-18 | medium   | chefs | Two `RenderFlex` overflows in a leaderboard row, same class as B001/B002/B016 — found by the new 320px / 2.0×-text-scale envelope test **before merge**, not in review. (a) `_Stats` builds each stat as a `Row` of icon + `Text`; a `Wrap` constrains each child to the wrap width, but an intrinsically-sized `Text` in a `Row` has no way to degrade, so large counts overflowed by 17px and then 85px. (b) The trailing score `Column` was unconstrained, so it took its intrinsic width; at 2.0× scale on a 320px phone that starved the `Expanded` chef badge beside it until `ChefBadge`'s own row — whose fixed floor is avatar + gap — overflowed by 8.8px. The second is the more instructive one: the widget that overflowed was not the widget at fault. | fixed | Stat labels wrapped in `Flexible` + ellipsis; the score column bounded by `ConstrainedBox(maxWidth: 96)` with both texts `maxLines: 1` + ellipsis. Regression tests pump a worst-case row (38-char name, `Master Chef`, six-figure counts, score `987654.5`) at 320/360/600px @ 2.0×. |

### Seed bugs (found applying Phase 18 to the hosted project, 2026-08-18)

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B024 | 2026-08-18 | high     | seed | Re-seeding an **already-seeded** database failed outright: `ERROR 42725: function seed_recipe(uuid, unknown, …) is not unique / Could not choose a best candidate function`. Adding `p_visibility` took `seed_recipe` from 16 arguments to 17-with-a-default, and `create or replace function` **cannot change a function's argument list** — so the old 16-arg overload survived alongside the new one, and every 16-argument call in `seed.sql` then matched both (the old exactly, the new via the default). `drop.sql` did carry the old signature, but `drop.sql` is a separate destructive script that a plain re-seed never runs. Hit on the hosted project, which had been seeded before the signature changed. | fixed | `seed.sql` now drops **every historical `seed_recipe` signature itself**, immediately before `create or replace`, instead of relying on `drop.sql`. Same guard shape belongs on any future seed-helper signature change. |

### Recipe-content bugs (found auditing `recipeData/data.json`, 2026-08-18)

| ID   | Date       | Severity | Area        | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ----------- | ----------- | ------ | ------------ |
| B025 | 2026-08-18 | medium   | recipe data | `recipeData/data.json` (the curated Secret Sauce Kitchen recipes, staged for import) carried one **exact duplicate** — `Spring Vegetable Tart` appeared twice, byte-identical — plus nine content defects that would have shipped as visible recipe text: `"1 ts"` for `1 tsp` (Teriyaki paprika); the galette's egg wash stored with `amount`/`item` **inverted** (`{"amount":"1 egg plus 1 Tbsp water","item":"beaten"}`); Teriyaki seasoning with salt that was never listed; the margarita listing `2 oz orange liqueur` that **no step uses**, listing `Simple syrup` as an ingredient while step 1 also *makes* it from the sugar + water already listed, and saying "tequila (if using)" for a required ingredient; `Raspberry Brownies` claiming 4 servings from an 8×8 pan and a step calling for a "chocolate and butter mixture" when the ingredient is cocoa powder; ratatouille saying "bell peppers" for one pepper and serving with crusty bread that was never listed. The duplicate would not have surfaced on import — `seed_recipe` dedupes on `(owner_id, title)`, so the second copy is silently swallowed rather than raising. | fixed | Deduped to 9 recipes and all nine defects corrected in place; item casing normalised to lowercase and `1 tbsp` → `1 Tbsp`. Structural causes (free-text `servings`, `amount` as one opaque string, no groups, no times) are **not** data bugs — addressed by the per-recipe schema in the same change set. |

| B026 | 2026-08-18 | medium   | seed        | Every helper in `supabase/seed.sql` was left callable as a PostgREST RPC. `seed_recipe` and `seed_ratings` both **write rows**, and `revoke execute` was never issued, so `anon` and `authenticated` held the default `PUBLIC` grant on them (confirmed: `proacl` was null on all four, i.e. default-to-PUBLIC). Not directly exploitable — both are invoker-rights, so RLS still filters what the caller can actually write, and `seed_ratings` inserting a rating attributed to a taster id would fail `ratings_write`'s `user_id = auth.uid()` check — but it violates the rule `0001_init.sql` already follows for `bump_count` / `recompute_recipe_rating` / `recompute_chef_stats`, and it puts seed-only machinery on the public API surface. Found while writing the equivalent revokes for `seed_recipe_v2`. | fixed | `revoke execute … from public` (and from `anon, authenticated`, guarded on the role existing) added to `seed.sql` for `seed_recipe`, `seed_ratings`, `seed_taster_ids`, `seed_chef_ids`, and to `seed_recipes.sql` for `seed_recipe_v2` / `seed_recipe_v2_ratings`. Verified: `proacl` on all six is now `postgres=X/postgres`. |

**Why the defects clustered where they did.** Every one of them is an ingredient the *prose*
knows about but the *list* does not, or vice versa. The flat `{"amount","item"}` shape gives
nothing to cross-check against: an ingredient that no step mentions and a step that names an
ingredient nobody listed are both well-formed JSON. The per-recipe schema does not fix this by
itself either — `tool/recipes.dart` is where a lint for it belongs.

**Why the Phase 18 verification missed it — a gap in the method, not just the code.** Every SQL
check that day ran one of two paths: `supabase db reset` (builds from scratch — the stale overload
cannot exist) or `drop.sql → create → seed` (drops both overloads first). Neither is the path a
deployed database actually takes. **Upgrade-in-place on an already-seeded database was never
exercised**, and that is the only path where this bug is reachable. Re-verified afterwards by
reconstructing the pre-Phase-18 world from git (`git show 2f2e6e0:supabase/…`), applying the new
migration on top, and confirming the committed seed fails (`exit 3`) while the fixed one succeeds
and stays idempotent across three consecutive runs. **Any change to `0001_init.sql` or `seed.sql`
should be tested on the upgrade path, not only on a fresh `db reset`.**

### Documentation bugs (found auditing `CLAUDE.md` against the code, 2026-08-18)

| ID   | Date       | Severity | Area | Description                                                                                                                                                                                                                                                                                                                                                                                                       | Status | Fix / Commit                                                                                                                                                                                                                                                                                       |
| ---- | ---------- | -------- | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B019 | 2026-08-18 | medium   | docs | `CLAUDE.md` "Conventions" said imports are "relative within a package", but `analysis_options.yaml` enables `always_use_package_imports` — every in-package import in `core` is `package:core/src/...`. Following the documented rule produces code that fails `melos run analyze`. Same section also listed the Postgres enum as `visibility` (real name: `recipe_visibility`) and named only 2 of the 4 enums.       | fixed  | `CLAUDE.md` now states package imports everywhere, lists all four enums, and links `enums.dart`.                                                                                                                                                                                                    |
| B020 | 2026-08-18 | medium   | docs | `CLAUDE.md`'s repository tree omitted the `src/` layer — it showed `packages/core/lib/models/`, but the real path is `packages/core/lib/src/models/`. It also claimed a `core/lib/utils/` directory that does not exist, and omitted `tool/`, `supabase/seed.sql`, `supabase/scripts/`, `.github/workflows/`, `.claude/skills/`, `apps/app/lib/widgets/`, and both `test/` directories. Same drift in `EXECUTION-PLAN.md` Phases 3–4. | fixed  | `CLAUDE.md` tree corrected and annotated; `docs/EXECUTION-PLAN.md` Phases 3 and 4 updated to the real `lib/src/...` paths. `docs/SDS.md` §2's layer table still uses loose non-`src/` paths (`packages/core/repositories`) — descriptive, left as is.                                              |
| B021 | 2026-08-18 | low      | docs | `CLAUDE.md` advertised Riverpod "+ `riverpod_generator`". The generator was a dev dependency in `core` and `app`, but **no `@riverpod` annotation existed anywhere** — every provider is hand-written. An agent reading this would add annotated providers, splitting the codebase across two provider styles.                                                                                                          | fixed  | Documented as hand-written-providers-only, and the unused `riverpod_annotation` / `riverpod_generator` deps were removed from both `packages/core` and `apps/app`. `apps/app` also lost `build_runner` — it had no annotated sources, so all codegen now runs in `packages/core` alone. Re-verified: bootstrap, build_runner (21 outputs, core only), analyze (clean ×3), test (13 passing). |

---

### Schema verification run (2026-08-18, local Supabase stack)

`supabase start` + `supabase db reset` against the local stack (Docker images already cached),
then RLS behavior exercised with `set local role` / `request.jwt.claims`:

| Check                                                       | Result                                        |
| ----------------------------------------------------------- | --------------------------------------------- |
| `0001_init.sql` + `seed.sql` apply cleanly                  | pass (no errors; 6 recipes seeded)            |
| Rating aggregates from seed                                 | pass — e.g. cookies 4.88/8, guacamole 4.50/3  |
| `recipes_popular()` order                                   | pass — descending weighted score              |
| Non-owner likes a recipe (B011)                             | pass — `like_count` 128 → 129                 |
| Non-owner rates + re-rates (upsert)                         | pass — avg 4.69 → 4.44, count unchanged at 8  |
| Removing a rating                                           | pass — avg 4.64, count 7                      |
| Owner rating own recipe                                     | rejected by RLS ✔                             |
| Rating `4.3` / `0`                                          | rejected by check constraint ✔                |
| Anonymous rating                                            | `permission denied` ✔                         |
| Private recipe: other user / anon                           | invisible; visible after `recipe_shares` row ✔ |
| Recipe flipped to `public`                                  | anon can read ✔                               |
| PostgREST `/rpc/recipes_popular` as `anon`                  | pass — `rating_avg` is a JSON number          |

The hosted project still needs the updated `0001_init.sql` re-applied (idempotent) to pick up
`recipe_ratings`, the B011 `security definer` fix, and the B013 grants.

### View-count verification run (2026-08-18, local Supabase stack)

`supabase start` + `supabase db reset`, then the B012 trigger exercised via `set local role` /
`request.jwt.claims` against a seeded recipe (baseline `view_count` 3050):

| Check                                                        | Result                                     |
| ------------------------------------------------------------ | ------------------------------------------ |
| Signed-in non-owner logs a first view                        | pass — 3050 → 3051                         |
| Same user re-views 5× (refresh loop)                         | pass — unchanged at 3051                   |
| A second distinct signed-in user                             | pass — 3051 → 3052                         |
| Anonymous views ×20 (`user_id is null`, role `anon`)         | pass — inserted, counter unchanged at 3052 |
| User forging a view as another `user_id`                     | `new row violates row-level security policy for table "recipe_views"` ✔ |
| Log vs. counter after the run                                | 27 rows / 2 distinct users → counter +2 ✔  |
| `0001_init.sql` re-applied on top                            | exit 0, `view_count` unchanged (idempotent) |
| `recipes_trending()` / `recipes_popular()` after the change  | pass — both still ordered correctly        |
| **Two concurrent first-views, one account** (overlapping transactions) | pass — 2100 → 2101, 2 log rows, 1 bump |
| Same race with `pg_advisory_xact_lock` removed (control)     | 940 → **942** — confirms the race is real and the lock is what prevents it |
| `recipe_views` index topology after re-apply                 | `recipe_views_pkey`, `recipe_views_recipe_user_idx` (redundant `recipe_views_recipe_idx` dropped) |

### B022 verification run (2026-08-18, local Supabase stack)

`supabase start`, then a fixture recipe written directly into the DB in known-ascending order
(2 ingredient groups / 5 ingredients, 2 step groups / 6 steps) and a local-only account owning it
so `update()` was reachable. A throwaway harness under `apps/app/test/` (**not committed** — it
needs a live database and CI has no DB job) drove the real `SupabaseRecipeRepository`.

Chrome is not installed on this machine, so the browser eyeball-check was replaced by this
harness, which exercises the same code path the screen does and asserts stored order too.

| Check                                                                        | Result                                          |
| ---------------------------------------------------------------------------- | ----------------------------------------------- |
| `.order()` default in postgrest 2.9.1 (`postgrest_transform_builder.dart`)   | `bool ascending = false` — root cause confirmed  |
| Raw PostgREST `order=sort_order.desc` vs `.asc` on the fixture               | `SG-B, SG-A` / `step-b3…` vs `SG-A, SG-B` / `step-a1…` |
| `getById()` group + step + ingredient order, with the fix                    | pass — `SG-A, SG-B`, steps `a1→b3`, ingredients `a1→b2` |
| `update()` ×1 — returned model **and** stored `sort_order`/`step_order`      | pass — unchanged                                |
| `update()` ×2 — same checks again (catches the flip-flop)                    | pass — unchanged                                |
| **Control:** same harness with the four `ascending: true` flags removed      | **fails** — `['SG-B', 'SG-A']`; the check is not vacuous |
| `melos run analyze` (core, design_system, app)                               | clean ×3                                        |
| Hosted audit: `recipe_versions` per recipe                                   | 6 recipes, all `v1 'Seeded recipe'` — never app-edited |

### Chefs / leaderboard verification run (2026-08-18, local Supabase stack)

`supabase db reset` (migrations + seed), then the trigger and RLS behavior exercised with
`set local role` / `request.jwt.claims`, the RPC called over PostgREST as `anon`, and the Dart
side driven by a throwaway harness under `apps/app/test/` (deleted after — CI has no DB job).

| Check                                                                       | Result |
| --------------------------------------------------------------------------- | ------ |
| `0001_init.sql` + `seed.sql` apply cleanly on a fresh DB                     | pass — 15 recipes, 16 profiles |
| Seeded tier ladder                                                          | pass — d1 21000 `master_chef`, Kitchen 10189 `head_chef` (unassisted), d2 5500 `head_chef`, d3/d7 1200 `sous_chef`, d4 100 `line_cook`, d5 0 `home_cook` |
| Score of **exactly 100** (d4)                                               | `line_cook` ✔ — thresholds are inclusive |
| Multi-recipe chef (d1, two recipes)                                         | pass — 21000 = sum of both, not either alone |
| `dense_rank` tie (d3 vs d7, 1200 each via different mixes)                  | pass — both rank 4, next rank is 5, no gap |
| **Private-only chef (d6)**                                                  | pass — 5000/2000/10000 on a private recipe would score 27000; actual `chef_score` 0, `home_cook`, absent from the board |
| Tasters (no recipes)                                                        | absent from the board ✔ (`public_recipe_count > 0` filter) |
| **B011 definer check:** non-owner likes a chef's recipe                     | pass — owner score 100 → 103 |
| Non-owner save + view                                                       | pass — 103 → 108.2 |
| Unlike returns the score                                                    | pass — 108.2 → 105.2 |
| Recipe flipped `private`                                                    | pass — 5500 → 0, `home_cook`, drops off the board; flipping back restores 5500 / `head_chef` |
| Recipe deleted                                                              | pass — 21000 → 13100 (the deleted recipe was worth 7900) |
| Recipe changes owner                                                        | pass — both old and new owner recomputed |
| `anon` calls `chefs_leaderboard` (psql role **and** PostgREST `/rpc/`)      | pass — 7 ranked rows |
| `anon` calls `recompute_chef_stats`                                         | `permission denied for function` ✔ |
| **Double-apply** of `0001_init.sql` + `seed.sql` on top                     | pass — no errors, byte-identical chef stats, no duplicate rows |
| `drop.sql` → re-create → re-seed                                            | pass — no leftover functions or types; exactly **one** `seed_recipe` overload (old 16-arg signature dropped) |
| **Upgrade in place** — pre-Phase-18 DB (old migration + old seed from `2f2e6e0`), then new migration, then new seed | **initially failed — B024.** After the fix: new seed exits 0, leaderboard identical to the from-scratch result, and two further re-runs stay stable at 15 recipes / 16 profiles / 64 ratings / 1 overload. Control (seed as committed in `a330370`) still exits 3, so the check is not vacuous |
| Owner embedding, bare `owner:profiles(...)`                                 | **PGRST201** — ambiguous, five relationships; the FK hint is mandatory (see B-note in SDS §10.5) |
| Owner embedding, `owner:profiles!recipes_owner_id_fkey(...)`                | pass on the `recipes` table, `recipes_popular` / `_trending` / `_search`, and nested in `recipe_shares` |
| Dart decode, signed out — leaderboard order, tiers, ranks, exclusions       | pass |
| Dart decode — `chef_score` (Postgres `numeric`) as `double` (Gotcha 11)     | pass — generated as `(json['chef_score'] as num?)?.toDouble()` |
| Rating write does **not** rewrite the owner's `profiles` row (`is distinct from` guard) | pass — `xmin` unchanged across a rating insert; a like still moves 100 → 103 |
| `melos run analyze` / `melos run test --no-select`                          | clean ×3 / 35 passing |

The hosted project still needs `0001_init.sql` + `seed.sql` re-applied (both idempotent) to pick
up the chef columns, trigger, and leaderboard RPC.

## Known environment limitations (not bugs)

- **Verified 2026-08-18** on Flutter 3.44.8 / Dart 3.12.2 (SDK at `C:\src\flutter`, melos 6.3.3):
  `melos bootstrap`, `melos run build_runner` (21 outputs), `melos run analyze` (clean in all three
  packages), `melos run test` (all passed), and `flutter build web --release` all succeed.
- Host is Windows 11 **ARM64**; Flutter 3.44.8 has no ARM64 Dart SDK and falls back to x64. Works.
- Windows **Developer Mode** is off on this machine. Flutter 3.44.8 bootstraps fine without it, but
  some Flutter versions fail with `Building with plugins requires symlink support` — enable it via
  `start ms-settings:developers` if that appears.
- **Runtime verified 2026-08-18** with real credentials in `apps/app/env.local.json`:
  `flutter run -d web-server --web-port 8080 --dart-define-from-file=env.local.json` serves the app
  (HTTP 200), Supabase Auth answers on `/auth/v1/health` (GoTrue v2.190.0), and PostgREST returns
  the seeded public recipes via `/rest/v1/recipes` — so the schema, RLS anon-read policy, and
  `supabase/seed.sql` are all applied on the hosted project.
- Not yet verified: signed-in flows (create / edit / fork / version history), image upload to
  Storage, and anything on Android or iOS.
- Web runs via the `web-server` device (`http://localhost:8080`); Chrome is not installed and
  Edge's auto-launch debugger fails, so use `-d web-server` or `-d windows`.
- Supabase project is provisioned (hosted); the consolidated `supabase/migrations/0001_init.sql`
  is idempotent and applied.

### UI bugs (found porting RecipeCard v2, 2026-08-18)

| ID   | Date       | Severity | Area          | Description                                                                                                                                                                                                                                                                                                                                 | Status | Fix / Commit                                                                                                                                                                                                                                                                              |
| ---- | ---------- | -------- | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B026 | 2026-08-18 | low      | design_system | `RecipeCard`'s metadata row gave **both** children a flex (`Expanded` group + `Flexible` badge), and `RenderFlex` splits free space by flex factor without looking at content — so the badge reserved half the row no matter how short its label. At one-column widths the rating pill was truncated to `4…` with ~150px sitting empty next to "Easy", and the badge never sat flush right the way the mockup has it. Predates v2 (shipped with B016's fix); found in a browser screenshot of Discover at 390px. | fixed  | Badge is now the only non-flex child, capped at half the row via `LayoutBuilder` + `ConstrainedBox`. A bare intrinsic badge with no cap overflows by 1px at 276px / 2.0× — the existing envelope tests caught that immediately, which is why the cap is there rather than a plain intrinsic child. |

### Tooling bugs (found running the documented commands, 2026-08-18)

| ID   | Date       | Severity | Area    | Description                                                                                                                                                                                                                                                                                                                | Status | Fix / Commit                                                                                                                                                                                                                                                                              |
| ---- | ---------- | -------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B027 | 2026-08-18 | low      | tooling | `melos run format` **breaks** `melos run analyze`. `dart format` picks its style from the **package's language version**, not the SDK's: every pubspec here declares `sdk: ">=3.4.0 <4.0.0"`, and 3.4 < 3.7, so the pinned Dart 3.12 formatter emits the **legacy short style** — continuation-indented argument lists with no trailing comma (`const _Body(\n      {required this.recipe, …})`). `analysis_options.yaml` enables `require_trailing_commas`, which flags every one. One run reformatted 20 files and produced 12 new `info` diagnostics across `core` and `design_system` — a red CI `analyze` job from running the repo's own format script. The committed tree is in the newer **tall** style, so it is format-dirty under the current constraint but lint-clean. | fixed  | **OPT-T4.** The `sdk:` lower bound went to `>=3.7.0` in all four pubspecs, so `dart format` emits the **tall** style, and the repo was reformatted once, whole (103 files). `melos run format` followed by `melos run analyze` is now green — verified in that order, which is the only check that means anything here. Lowering the bound below 3.7 re-arms it. |

### Tooling bugs (found screenshotting the new top navigation, 2026-08-19)

| ID   | Date       | Severity | Area    | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ------- | ----------- | ------ | ------------ |
| B028 | 2026-08-19 | low      | tooling | `flutter run -d web-server` (debug) renders for the **first** browser session only. The first headless page load drew the app; every later load of the same server — new browser, same URL — showed a blank white page with no `pageerror` and no console error, only the DDC bootstrap logs (`DDC is about to load 949/949 scripts`). Consistent with the web-server device's single debug-service client, which the run's own banner hints at ("requires the Dart Debug Chrome extension"). Costs real time, because the failure looks like an app crash rather than a dev-server limitation. | wontfix | Not worth fixing in the tool. For screenshots use a **release** bundle instead: `flutter build web --release --dart-define-from-file=env.local.json`, serve `apps/app/build/web` statically (`npx serve -l <port> .`), and drive that — it renders on every load, in any number of sessions. Documented in `README.md` troubleshooting. Second gotcha found alongside it: the web build uses the **hash** URL strategy, so a deep link is `http://host/#/discover`; `http://host/discover` silently serves `index.html` and lands on `/`. |

### UI bugs (found porting the Chefs page v2, 2026-08-19)

| ID   | Date       | Severity | Area          | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ------------- | ----------- | ------ | ------------ |
| B029 | 2026-08-19 | high | design_system | `ChefStandingCard`'s tier spine was a stretched `Row` child (`CrossAxisAlignment.stretch`), the usual way to make a full-height bar. The card lives in a `ListView`, so its height is **unbounded** during layout and `stretch` passes `h=Infinity` to the spine: `BoxConstraints forces an infinite height`, followed by a cascade of `RenderBox was not laid out: 'hasSize'`. Eleven app tests failed at once — including three in `chefs_routing_test.dart` that have nothing to do with the card, which is what made the root cause non-obvious from the failure list. | fixed | Spine is a `Positioned(left/top/bottom, width: 6)` child of a `Stack` whose other child sizes the card. `IntrinsicHeight` also works but adds a layout pass to every one of 50 rows. General rule: `CrossAxisAlignment.stretch` on a `Row` is only safe where the height is already bounded. |
| B030 | 2026-08-19 | medium | app | A modal opened from a shell screen renders **under** the shell chrome. `AppShell` is a `Scaffold` that owns the bottom `NavigationBar` and the FAB and wraps the shell's inner navigator, so `showModalBottomSheet` / `showDialog` attach to *that* navigator: on mobile the FAB and nav bar painted on top of the expanded chef card's content, and on web the scrim stopped below the top navigation bar, which stayed undimmed. Caught in a screenshot, not by a test — a widget test pumps the screen without the shell. | fixed | `useRootNavigator: true` on both. Any future sheet or dialog opened from a `ShellRoute` screen needs the same. |
| B031 | 2026-08-19 | low | design_system | Board stats read `1 recipes` / `1 likes`. The stat chips interpolated a hard-coded plural noun. | fixed | Plural drops its trailing `s` at a count of exactly 1. |
| B032 | 2026-08-19 | low | design_system | On a phone the chef name and the dense `TierChip` shared one `Row`, so the chip took roughly a third of the text column and almost every name ellipsised to `Amara…` / `Secret…` / `Greta Li…`. The 320/360px envelope tests passed throughout — nothing overflowed, the text just degraded to uselessness, which is exactly what an overflow test cannot see. | fixed | Compact stacks the name above the chip (matching the design's own mobile frames) and drops the name to `titleSmall`. |

### Tooling bugs (found applying Phase 22's schema change to the hosted project, 2026-08-19)

| ID   | Date       | Severity | Area    | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ------- | ----------- | ------ | ------------ |
| B033 | 2026-08-19 | medium | tooling | `melos run db:*` cannot run on this machine. Two independent reasons: (1) `tool/db.dart` shells out to **`psql`, which is not installed** — the only client here is the one inside the `supabase_db_secret-sauce` container; (2) piping the file through that container fails against the hosted project because `db.<ref>.supabase.co` resolves **IPv6-only** and the container has no IPv6 route (`connection to server at "db.<ref>.supabase.co" (2600:…), port 5432 failed: Network is unreachable`) — the Windows host reaches it fine, so this is a container-networking limit, not a Supabase one. | fixed | Use the **Session pooler** host (IPv4) with the container's `psql`: `Get-Content supabase\migrations\0001_init.sql -Raw \| docker exec -i supabase_db_secret-sauce psql $u -v ON_ERROR_STOP=1 -f -`. Applied clean (exit 0) on 2026-08-19. Two sub-traps: the pooler requires user `postgres.<project-ref>`, not bare `postgres`; and a dashboard password reset takes a moment to propagate, so an auth failure straight after a reset is not proof of a wrong password — test with `psql $u -c "select 1"` before re-running a migration. Permanent fix is installing a real `psql` and putting it on PATH, which would make the documented `melos run db:create` work as written. |
| B034 | 2026-08-19 | high | security | `SUPABASE_DB_URL` (the **postgres superuser** connection string) was added to `apps/app/env.local.json`, which is passed to every build via `--dart-define-from-file`. `CLAUDE.md` marks that variable "shell env only — never a file" for exactly this reason: the file is git-ignored, so nothing reached the repo, and Dart only emits defines the code reads — but that is a compiler optimization, not a boundary, and one `String.fromEnvironment` would publish a full-access credential into a web bundle. | fixed | **OPT-S7.** The key is gone from `apps/app/env.hosted.local.json` (where it had migrated when the hosted creds were parked); no dart-define file carries it now. It lives in `db-url.local.ps1`, dot-sourced per shell, with `db-url.example.ps1` committed as the template and `*.local.ps1` / `**/*.local.ps1` added to `.gitignore` (negated for `*.example.ps1`). A dot-sourced script rather than a Windows **user** environment variable on the owner's call: a global value is inherited by every other repo on the machine, and `tool/db.dart` fires at whatever `SUPABASE_DB_URL` says with no confirmation and no prod guard (Gotcha 7) — per-shell, per-project keeps a cross-project mistake off the table. A `.ps1` also cannot be handed to `--dart-define-from-file` by accident, which a stray `.json` can. The committed local copy defaults to the **local** stack with the hosted pooler URI commented out, so aiming `db:*` at production is deliberate. Verified: `git check-ignore` confirms both local files ignored and the example tracked; no `SUPABASE_DB_URL` remains in any `apps/app/*.json`; dot-sourcing sets the variable. **Rotation not required** — `git log --all -S` over the full history finds 0 commits containing the project ref or the password, so the credential never left the machine. |

### Recipe-editor bugs (found asking why manual recipes have no time/temperature tags, 2026-08-19)

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B035 | 2026-08-19 | high | recipe_editor | The editor's draft types modelled a step as **its text and nothing else** (`EditStep.fromModel(s) => EditStep(text: s.text)`), and an ingredient without `note` / `is_optional`. Two consequences, the second much worse than the first. (1) **Unreachable fields**: `temperature`, `duration_minutes`, `tip` and `image_url` could not be entered when creating a recipe, which is why only the 14 `recipeData/` recipes — inserted by `seed_recipe_v2` straight from JSON, bypassing the editor — carried the time/temperature chips that `recipe_detail_screen.dart:486-502` renders. (2) **Silent data loss on edit**: open such a recipe, change the title, save — `_load()` keeps only the step text, `toModel()` emits nulls for the rest, and `RecipeRepository.update()` **deletes every `step_group` / `ingredient_group` and re-inserts** what it was handed ([recipe_repository.dart:151](../packages/core/lib/src/repositories/recipe_repository.dart)). Every temperature, timer, tip, step image, ingredient note and optional flag is gone, with no warning and no field the user ever touched. Applies to all 14 Kitchen recipes and every fork of one; the prior `recipe_versions` snapshot survives, so it is recoverable by hand but not by the UI. Same shape as B022's secondary damage — the editor round-trip is the repo's sharpest edge, because `update()` is delete-then-reinsert rather than a patch. | fixed | `EditStep` gained `duration` / `temperature` / `tip` controllers plus a pass-through `imageUrl` (no per-step picker yet — the value is carried verbatim so a save cannot drop it); `EditIngredient` gained `note` and `isOptional`. Both reveal the extra inputs behind a per-row disclosure that **starts open when the loaded row already uses them**, so existing content can never be hidden and then dropped. `recipe_editor_test.dart` asserts the full `fromModel → toModel` round-trip field by field, which is the test that fails if a future field is added to the core model and not to the draft. |
| B036 | 2026-08-19 | medium | recipe_editor | Pre-existing, found by the new envelope test rather than in use: `DropdownButtonFormField<Difficulty>` overflowed by 101px at 360px / 2.0× text scale. A dropdown's internal `[label, arrow]` row is intrinsic and `isExpanded` defaults to false, so half of a phone row is not enough for "Medium" plus the arrow. Same class as B001/B002/B016/B023/B026. A second, smaller one surfaced on top of it once the ingredient row gained a disclosure button: at 320px the row's fixed children (64 + 72 sized fields, two 48px icon buttons, two gaps = 248px) exceeded the 240px the card leaves, overflowing by exactly 8px — and even where it fit, the `Expanded` name field was left a few pixels wide, the B032 failure mode that an overflow test cannot see. | fixed | `isExpanded: true` on the dropdown with `maxLines: 1` + ellipsis on each item label. The ingredient row splits into two lines below `248 + 120 × textScale` px — quantity and unit keep their sized boxes on the first, the name and the action buttons share the second, so the name always gets the full width. The "Optional" control is a `Checkbox` + `Flexible` label, not a `FilterChip`: a chip sizes to its label and "Optional" at 2.0× is wider than a 320px phone leaves. Envelope tests at 320/360/600px @ 2.0× assert each stage — collapsed form, ingredient details, step details — separately, so a failure names the row. |

### Chefs page v3 bugs (found building the hero / spotlight card / rails, 2026-08-20)

All four are layout, all four were found by a test or a review rather than in use, and three are
the same family as B001/B002/B016/B023/B026: **a `Row` or `Column` whose children cannot give
ground.** The new axis this phase added is that a page can be over-budget in *height* the same way
a card row is over-budget in width, and for the same reason — something up the tree fixed the space.

| ID   | Date       | Severity | Area          | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ------------- | ----------- | ------ | ------------ |
| B037 | 2026-08-20 | medium | chefs | The chefs page overflowed **twice** at 1000px / 2.0× text scale: the outer `Column` by 140px vertically and the hero's three-part `Row` by 137px horizontally. Root cause is one thing seen from two sides. The two-column layout is `Column(hero, Expanded(Row(panel, rails)))`, so the hero is a **non-flexible** child taking its intrinsic height; the hero's own row then squeezed the identity block into ~168px, the strapline wrapped to seven lines at 28px text, and the hero came out ~1284px tall against a 1144px viewport. The `Expanded` below it cannot absorb a sibling that is already taller than the parent. Caught by the new page envelope test at 1000px, and by `top_nav_bar_test`'s existing 1000/1400px envelopes, which pump the real shell — so this would have broken two suites, not one. | fixed | Two bounds, each stated where it applies. The hero stacks its three parts instead of rowing them below `900 × textScale` px, so the identity block never gets a column too narrow to hold a sentence. The page drops to its single-scroll layout above `ChefsScreen.maxTwoColumnTextScale` (1.3) — fixed-height columns under a non-scrolling hero must all fit the viewport at once, and past that scale they cannot. `context.textScale` in `adaptive.dart` is the shared measurement. |
| B038 | 2026-08-20 | low | design_system | `ChefSpotlightCard`'s header paired an `Expanded` name with a `Flexible` score. Two flex children split the row 50/50 **whatever the content**, so at 280px `Secret Sauce Kitchen` truncated at 104px while the 75px score left 29px of dead space beside it — the name, which is the card's identity, lost width that nothing used. No overflow, so no envelope test could see it; this is the B032 failure mode (degrade-to-uselessness) in a new place. Found in review, not by a test. | fixed | The score is non-flex inside a `ConstrainedBox(maxWidth: constraints.maxWidth / 3)` with a `FittedBox`, and the name takes the remainder — the same shape `RecipeCard` uses for its difficulty badge (B016). The driver row's points column had the identical split and got the identical fix. The regression test asserts the **ratio** (name wider than score), not a pixel count: `flutter test` renders in a fixed-width font far wider than Roboto, so an absolute width would pin the harness rather than the layout. |
| B039 | 2026-08-20 | low | design_system | The rank pill in the spotlight card's portrait overlay was a bare, non-flexible child of a `Row`. Non-flex children of a flex are laid out with an **unbounded** main axis, so `RANK 128` at 3.0× text scale ran 13px past the portrait's edge. Only visible above the growth clamp in `spotlightCardHeight`, which is why the 2.0× tests passed — the 3.0× case exists precisely because iOS accessibility scaling reaches it. | fixed | Wrapped in `Flexible` + `FittedBox(scaleDown)`, and the serial beside it promoted from `Flexible` to `Expanded` so it takes the slack. The rank shrinks rather than clipping — a smaller number beats a cut one, the same call the score column makes. |
| B040 | 2026-08-20 | low | chefs | The Popular rail rendered a shelf of `SpotlightCardPlaceholder`s whenever the board was empty, without distinguishing *loading* from *loaded and genuinely empty*. On a database with no ranked chefs the page therefore showed ten placeholder cards under "Popular chefs" — reading as "still loading, forever" — directly beside the board panel's `No chefs yet` empty state, which says the opposite. | fixed | Three states, not two: placeholders only while `async.isLoading`, real cards when there are chefs, and the whole rail omitted when the board has loaded empty. Pinned by a test that asserts the shelf is absent and the two placeholder rails remain. |
| B041 | 2026-08-20 | low | chefs | Every segment of the hero's time filter and the board's sort tabs was wrapped in a `Tooltip` whose message was `''` for the enabled ones. An empty `Tooltip` still opens an empty box on hover, so the two working controls each grew a blank popup — the wrapper was meant to explain only the deliberately inert segments. | fixed | `notYetTooltip(enabled:message:child:)` returns the child untouched when the control works, and wraps it only when it does not. |

### Seed / database bugs (found while reseeding the local stack, 2026-08-20)

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B042 | 2026-08-20 | low | seed | `supabase/scripts/drop.sql` lists all three historical `seed_recipe` signatures plus `seed_taster_ids` / `seed_chef_ids` / `seed_ratings`, but has **zero** entries for `seed_recipe_v2` and `seed_recipe_v2_ratings` — the two helpers `seed_recipes.sql` has created since Phase 19. Both are `language plpgsql`, so their bodies are opaque to the dependency tracker and the `drop table ... cascade` block above does not take them either: after `melos run db:drop` they survive as functions over dropped tables. Harmless **today**, because `seed_recipes.sql` recreates them with `create or replace` at an unchanged signature. It stops being harmless the first time that signature changes, which is the exact trap B024 already cost a debugging session: `create or replace` cannot change an argument list, the stale overload survives a full `drop → create → recipes` cycle, and every call then matches both (`42725 ... is not unique`). The generated file carries a "when this one changes, add its argument list below" comment for its own in-file drop block; `drop.sql` had no corresponding entry to extend. Found by reading the drop output during a local reseed — no test covers `drop.sql`, and neither of the two paths anybody runs (`supabase db reset` builds from scratch; `drop → create → seed` drops first) can surface it, which is Gotcha 6 restated. | fixed | Added both signatures to `drop.sql` with a comment stating *why* `cascade` does not cover them, so the next signature change extends the list rather than rediscovering the rule. Verified: `drop → create → seed → recipes` leaves six seed helpers, and a second `drop` now removes all six. |

### Simulation findings (Phase 24, 2026-08-20)

Every one of these was surfaced by `supabase/sim/3_sim_verify.sql`. None was visible at 23 recipes,
and none would have failed a `melos run analyze` or a `flutter test` run.

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B043 | 2026-08-20 | medium | chefs | **The `chef_tier` thresholds are not calibrated to any realistic population.** `master_chef` needs 20,000 points and the seeded demo chef `d1` clears it with 4,000 likes + 1,600 saves across **two** recipes. Against the simulated 1,000-account platform that is four likes per recipe from *every account in existence*; the strongest organically-generated chef (45 public recipes, 7,885 distinct viewers, 848 likes, 625 saves) reaches **7,246** — `head_chef`. So the hand-written demo numbers describe a platform perhaps 50× larger than the seed simulates, and the tier ladder shown on `/chefs` is effectively a two-rung ladder for any real early-stage userbase: 952 of 1,000 simulated accounts sit at `home_cook` and one reaches `head_chef`. Not a code defect — the formula and thresholds behave exactly as `chef_score()` / `chef_tier_for()` specify (verified by checks A5/A6). It is a **product calibration** question that only became answerable once there was a population to measure against, which is what SDS §10.8 predicted when it called the thresholds "provisional product numbers; expect retuning once real data exists". | open | Not fixed — retuning tiers is a product decision, not a bug fix, and the backfill-on-apply in `0001_init.sql` makes it a one-line change when taken. `3_sim_verify.sql` asserts `master_chef` only at the `large` preset and says why, so nobody is tempted to inflate the exposure model until the number appears. |
| B044 | 2026-08-20 | high | seed | Three defects in the sim generator, all of which produced a **successful** run with quietly wrong data, and all caught only by the assertion suite. (1) **Non-deterministic time anchor** — every timestamp was drawn as `from + (to - from) * u` with `to = now()`, so re-running after a `db:drop` recomputed each recipe's `created_at` *later* than the value `sim.recipe` had already recorded, while `recipe_versions` still derived from the registry: all 1,671 versions ended up dated before their own recipe (check C4). (2) **Lossy view ids** — built from `hashtextextended(actor_id) % 100000`, which collides between two viewers of the same recipe; the colliding row was swallowed by `on conflict (id) do nothing`, leaving 6 likes whose view did not exist (check B4). (3) **Over-constrained viewer eligibility** — the viewer was required to have signed up *before the recipe existed*, which both says a user who joined last week can never read a two-year-old recipe and silently discarded ~80% of draws rather than resampling, costing the star chefs most of their reach. | fixed | (1) `epoch_end` is pinned into `sim.config` on first generate and cleared by the teardown, and every `now()` in the generator body now reads it — zero remain. (2) A `row_number()` over the view-pair table gives a dense unique id. (3) The constraint moved to the view *timestamp*: `since = greatest(recipe.created_at, viewer.created_at)`. |
| B046 | 2026-08-20 | low | routing | **`/` was an orphaned page.** The Home landing screen had no entry point in either navigation chrome: the web brand mark navigates to `/discover` ([top_nav_bar.dart:175](../apps/app/lib/routing/top_nav_bar.dart#L175)), and `kMobileDestinations` has no Home slot. The only ways in were a cold start (`initialLocation: Routes.home`) and the two sign-out handlers, both of which dropped the user on a page with no way back into the app except its own buttons. It also duplicated the sign-in/sign-up affordances the top bar already carries. Not a crash — a dead surface that still had to be maintained, and a second first-impression to keep in sync with Discover. | fixed | Retired by decision (2026-08-20). `/` is a redirect-only `GoRoute` onto `/discover`; `initialLocation` moved to `/discover` for the mobile/desktop cold start where the platform reports no route. Both sign-out handlers go straight to `/discover`. `apps/app/test/widget_test.dart` — previously the HomeScreen widget test — now asserts the redirect and that the landing copy is gone. |
| B045 | 2026-08-20 | medium | seed | `sim.pick_dish()` is defined in `0_sim_schema.sql` but reads `sim.dish`, which `1_sim_dishes.sql` creates — and the run order is 0 then 1. Postgres validates a `language sql` body at creation time, so the **first ever apply on a clean database** failed with `relation "sim.dish" does not exist`. Invisible on any machine where an earlier run had already created the table, which is why it survived several green end-to-end runs; it only appeared once `drop schema sim cascade` was used to simulate a fresh machine. Same class as Gotcha 6 — the convenient paths do not exercise the one a new environment takes. | fixed | `sim.dish` is declared `create table if not exists` in **both** files with a comment saying why the duplication is load-bearing, so whichever runs first wins. Verified by dropping the schema entirely and running the full reset from zero. |

### RecipeCard follow-ups (2026-08-20, from the Claude Design review of the v2 card)

Both were found by looking at a real grid, not by a test — neither overflows, and every envelope
suite stayed green through both. Same family as B032/B038: **degrade-to-wrong, not crash.**

| ID   | Date       | Severity | Area          | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ------------- | ----------- | ------ | ------------ |
| B047 | 2026-08-20 | low | design_system | The title banner was **intrinsically sized**, so its height was whatever the name needed: a one-line title gave a ~44px band and a two-line title a ~68px one. Side by side in a grid row that reads as a layout error — the covers of neighbouring cards start at different y, the fold between banner and photo zig-zags across the row, and the cards stop looking like one component. The card's fixed 352px height hid it from every test: the cover simply absorbed the difference, exactly as designed, so nothing overflowed and nothing failed. | fixed | The band is `kRecipeCardBannerHeight` (65 — two lines' worth) with the title vertically centred, applied as a **minimum** height scaled by `context.textScale`. Scaled, because a hard 65px clips two lines of 2.0× type; a minimum, because text that still needs more must grow the band rather than overflow. The `title banner` group in `recipe_card_test.dart` asserts one- and two-line titles share a centre at 1.0× and 2.0×, and that a longer title clamps at two lines instead of adding a third. |
| B049 | 2026-08-20 | low | design_system | **`RecipeCard` overflows at 3.0× text scale** — by 17px on the bottom at 288px wide with the standard worst-case content, measured against the pre-B047 card, so this predates the banner work. The card's height is fixed at 352 and only the cover is flexible; past ~2.5× the banner plus the footer alone exceed 352 and the cover has no height left to give. Not covered by any suite: the card's contracted envelope is 2.0×, and 3.0× is only reachable through iOS accessibility scaling (the same scale that surfaced B039 on the spotlight card). Found while measuring the B047 band, not by a test. | open | Not fixed — the honest fix is `ChefSpotlightCard`'s shape (let the tile grow with the text, as `spotlightCardHeight` does) which means `kRecipeCardHeight` stops being a constant and `recipe_grid.dart`'s `mainAxisExtent` has to be computed per text scale. That is a grid change, not a card change, so it is deliberately not folded into the design port. B047's band is clamped at `kRecipeCardBannerMaxScale` (2.0) so it does not make this worse: unclamped the same case overflowed by 48px, clamped it is 13px — better than the 17px baseline. |
| B048 | 2026-08-20 | low | design_system | `kRecipeCardMinWidth` was **264**, which is below the width the footer actually needs. The grid packs columns down to that floor, so a wide window bought its extra column out of the metadata row: at 264 the time + rating group ran out of space and `4.9 (8)` ellipsized. The row is built to degrade (B016/B026), and it did — quietly, at default text scale, on a perfectly ordinary window width. The truncation is invisible to the envelope tests, which assert *no overflow*, and unassertable in a widget test at all: `flutter test` renders in a fixed-width font far wider than Roboto, so a width comparison would pin the harness rather than the layout (B038's lesson). | fixed | Floor raised to **288**, the width at which `12h 45m` + `4.5 (1250)` + `Medium` all fit uncut — a column fewer is the cheaper trade. 1408px of grid now packs four cards at the 340 cap instead of five squeezed ones; `flow_grid_test` and `recipe_grid_test` re-pinned to the new counts, and the card envelope suites now run 264 (below the floor — must degrade, never overflow), 288, and 340 at 1.0× and 2.0×. |

### Audit findings (design & architecture audit, 2026-08-20)

Found by a full-codebase audit (Dart + SQL), not in use. Each was verified against the cited
source before being logged. Fixes are scheduled as **Phase OPT** tasks (ROADMAP) with detail in
EXECUTION-PLAN.

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B050 | 2026-08-20 | high | schema | **Server-owned columns are writable by their owner over PostgREST.** The grants block issues a blanket `grant insert, update, delete on all tables in schema public to authenticated` ([0001_init.sql:796](../supabase/migrations/0001_init.sql)), and `recipes_update` / `profiles_update` are **row**-scoped only (`owner_id = auth.uid()` / `id = auth.uid()`, :666/:656) with no column restriction. So an owner can `PATCH` their own recipe's `like_count` / `save_count` / `view_count` / `rating_*` directly — and `recipes_chef_stats` then *legitimizes* the forged counter by recomputing `chef_score` from it, moving the owner up the public leaderboard. A direct write to `profiles.chef_score` / `chef_tier` also sticks until the next engagement recomputes it. `_writablePayload` omitting these columns is client-side convention, not enforcement — CLAUDE.md's "never trust the client" rule is violated for exactly the columns the product ranks people by. Sibling of B011/B013: grants and RLS both said yes, and nothing looked at *which columns*. | fixed | **OPT-S1.** RLS filters rows and cannot filter columns, so the fix is at the grant layer: the block now revokes the blanket `insert, update` on `recipes` / `profiles` and re-grants explicit column lists mirroring `_writablePayload` / `ProfileRepository.updateMine` — every counter, aggregate, `current_version_id`, and timestamp excluded, plus `owner_id` on UPDATE so a recipe cannot be reassigned. The revoke must sit *after* the blanket grant, which a re-apply re-issues. `current_version_id` was the one column the client genuinely wrote (`_appendVersion` PATCHed it), so it became server-owned for real: new `recipe_versions_set_current` trigger (`security definer` — under column grants an invoker-rights UPDATE of an ungranted column raises `42501` rather than matching 0 rows), and the Dart write was deleted, saving a round trip. `2_sim_generate.sql` disables that trigger for its bulk version load, which sets the pointer set-based. Verified on the local stack against the `medium` sim population: as the owner, `like_count` / `view_count` / `rating_avg` / `current_version_id` / `chef_score` / `chef_tier` / `owner_id` writes and a counter-forging INSERT all fail `permission denied for table`; a full-column editor save, a profile save, and a normal create all succeed; appending a version moves the pointer with no client PATCH; `chefs_leaderboard` unchanged; `3_sim_verify.sql` `ALL CHECKS PASSED` on both the upgrade path (schema re-applied over 1,694 existing recipes) and a clean `supabase db reset` + regenerated sim. |
| B051 | 2026-08-20 | medium | recipe_detail | **Signed-out (or repeat) like/save taps misbehave on a signed-out-reachable screen.** The like/save buttons call `setLiked(recipe.id, liked: true)` unconditionally ([recipe_detail_screen.dart:186-207](../apps/app/lib/features/recipe_detail/recipe_detail_screen.dart)). Signed out, `_uid` throws `StateError` inside an unawaited async closure — no feedback, unhandled error (Gotcha 9's exact shape). Signed in, the action is **one-way**: `liked:` is always `true`, nothing reads whether the user already liked/saved, so the heart never fills (`_CountAction.activeIcon` is dead) and there is no unlike, though `setLiked(liked: false)` exists on the repository. | fixed | **OPT-S3.** A shared `_toggleEngagement` handler now backs both buttons: it returns early to `/auth` when `currentUserIdProvider` is null (so the repository is never reached signed-out), writes the **opposite** of the current state, and catches failures into a snackbar instead of dropping them. New `myLiked` / `mySaved` repository methods use `currentUser?.id` like `myRating` (Gotcha 9) and answer `false` signed-out; `myLikedProvider` / `mySavedProvider` watch `currentUserIdProvider` so signing in re-resolves them. `_CountAction` gained `active` and finally renders `activeIcon`, tinted, with a matching tooltip. Regression cover: `apps/app/test/recipe_detail_test.dart`, 5 tests — a signed-out like/save tap lands on `/auth` with **zero** repository writes, an unliked tap sends `true`, and an already-liked/saved tap renders the filled icon and sends `false`. Three of the five fail against the old code. |
| B052 | 2026-08-20 | high | recipe_editor | **A failed editor load renders an empty draft over an existing recipe — saving it would erase the recipe's content.** `_load()` is `try/finally` with **no catch** ([recipe_editor_screen.dart:76-106](../apps/app/lib/features/recipe_editor/recipe_editor_screen.dart)): if `getById` throws (offline, RLS, bad id), the exception escapes as an unhandled future error, `_loading` flips false, and the screen shows the blank "new recipe" form bound to an *edit* route. `update()` is delete-then-reinsert (Gotcha 11 / B035), so tapping Save on that blank form deletes every ingredient/step group and re-inserts nothing. Same data-loss family as B035, reached through an error path instead of a modelling gap. | fixed | **OPT-S4.** `_load()` gained a `catch` that records `_loadError`; `build()` returns an `ErrorView` with **Retry** before it can reach the form, so the empty draft is never rendered over a real recipe. A separate `_loaded` flag drives `_canSave` (`!isEditing || _loaded`), which disables the Save button and short-circuits `_save()` — belt and braces, in case a later refactor renders the form on an error again. The exit path was extracted to `_leave()` so the error screen can close without a "Discard changes?" prompt for changes that do not exist. `getById` is awaited before any field is touched, so a failure leaves the draft untouched rather than half-filled. Regression cover in `recipe_editor_test.dart`: a failed load renders `ErrorView` and **no** `TextFormField`, offers no Save button, and retry recovers into the real recipe; a successful load leaves Save enabled. The first three fail against the old code. |
| B053 | 2026-08-21 | high | schema | **Creating a recipe is impossible over PostgREST: `INSERT … RETURNING` on `recipes` is rejected by its own SELECT policy.** `create()` issues `.insert(payload).select().single()` ([recipe_repository.dart:136](../packages/core/lib/src/repositories/recipe_repository.dart)), which PostgREST sends as `INSERT … RETURNING`. Postgres applies the SELECT policy to the returned rows, and `recipes_select` is `can_read_recipe(id)` — a **`stable security definer`** function that re-queries `recipes` **by id**. A `stable` function reads the *statement* snapshot, which cannot contain the row that same statement is inserting, so it returns false and the insert dies with `new row violates row-level security policy for table "recipes"`. The owner-visibility test never gets a chance to pass. Every other policy on the table is fine because they compare the row's own columns; only this one takes a round trip back through the table. Undetected until now because `seed_recipe_v2` and the sim create recipes as `postgres` (RLS bypassed) and ROADMAP "Outstanding" records the signed-in create flow as never exercised on this machine. Found while running OPT-S1's acceptance matrix; **pre-existing** — reproduced identically with the pre-OPT-S1 blanket grants restored, and the error is an RLS error, not `42501`. `UPDATE`/`DELETE … RETURNING` are unaffected (the pre-image is in the snapshot), verified, so OPT-S2 is safe. | fixed | **OPT-S1a.** Inlined `recipes_select` to test the row's own columns — `visibility = 'public' or owner_id = auth.uid() or exists (select 1 from recipe_shares …)` — instead of calling `can_read_recipe(id)`. Column references resolve against the new row directly, so no snapshot lookup is involved; it also drops a `security definer` call per row scanned. `can_read_recipe(uuid)` stays for the child tables, which pass a *parent* id that genuinely needs the lookup. `shares_self_select` (`shared_with_user_id = auth.uid()`) means the inlined subquery still works under invoker rights. Verified on the local stack at sim `medium`: anon sees public/not private; owner sees own private; an unrelated user sees public/not private; a shared user sees the private recipe only after the share row exists and still cannot UPDATE it; child content of a shared recipe stays readable; and the create-with-`RETURNING` that defined the bug now succeeds. `3_sim_verify.sql` `ALL CHECKS PASSED`; all four `anon` RPCs (trending/popular/search/leaderboard) return rows. Bonus: the policy is **2.4× faster** on a Discover-style scan (15.7 ms → 6.5 ms) — the planner hoists the shares subquery into a single hashed SubPlan (`loops=1`) instead of calling a `security definer` function per row, and both `visibility='public'` and `owner_id = auth.uid()` short-circuit before it. |
| B054 | 2026-08-21 | medium | tooling/sim | **`db:reset` does not reset the simulation, and the mismatch fails the sim's own assertions.** `drop.sql` never touches schema `sim`, and it spares `auth.users` by design (Gotcha 7 / B018). So after a reset on a machine that has run the sim: the simulated `auth.users` rows survive, `0001_init.sql`'s B015 backfill re-creates a `profiles` row for every one of them, and `sim.actor` / `sim.recipe` still list a population whose recipes and engagement were just dropped. `2_sim_generate.sql` is additive and registry-driven, so it builds on top of that ghost — a `--preset=tiny` run after a `medium` one reported "60 users" from its config and "1000 actors, 1671 recipes" from the registry, and `3_sim_verify.sql` failed **E9** (`no simulated chef reached head_chef at 1000 users`) because tiny's engagement scale was spread across medium's population. A second reset in the same state fails **A7** instead (`no anonymous view rows`), since the log was dropped while the registry was not. Found while verifying OPT-T1's job sequence locally; invisible in CI, where every runner starts empty. | open (backlog BL-3) | Not fixed here — the fix is a decision, not a patch. Making `drop.sql` drop schema `sim` would strand 1,000 simulated `auth.users` rows with no registry to delete them by, which is worse (the teardown is registry-driven precisely so it never keys on a pattern). The right shape is `db:reset` running `sim:clean` **first** (`9_sim_teardown.sql`, which deletes exactly the registered rows), but that makes `db:reset` delete `auth.users` rows and therefore a `--yes`-gated destructive action — an owner-facing change. Workaround today: `melos run db:sim:clean -- --yes` before a reset. |
| B055 | 2026-08-21 | low | design_system | **The tier chip is unreadable on a recipe card's cover.** `ChefBadge` takes `onSurfaceImage` and used it for the **name only**; `TierChip` resolved its colour from `theme.brightness`, which is the *page's* brightness — still light — so on the card's `black.withValues(alpha: 0.55)` scrim it painted the light-mode shade (a dark colour, chosen to contrast with a *light* background) at 14% alpha. The chef's name was crisp white and the tier under it was dark purple on dark grey. Nothing overflowed, so every layout test passed and the 288px / 2.0x envelope suite stayed green; the defect only exists in colour. Found by OPT-T5's screenshot pass, on the first screenshot. | fixed | `TierChip` gained `onImage`: it resolves the **dark-surface** half of the tier's colour pair (the pairs already existed, for dark mode) and raises the fill to 28%, since 14% of a pale colour over black is the scrim again. `ChefBadge` passes `onSurfaceImage` straight through. Two tests in `chef_badge_test.dart` assert the resolved label colour on each surface — a checkable proxy for a property no widget test can see. Verified by rebuilding the web release and re-shooting the same viewport: before, `Master Chef` is a dark smudge; after, it reads. |
| B056 | 2026-08-21 | low | recipe_detail | **Like/save counters were ungrouped.** The detail screen's counter buttons rendered `Text('$count')`, so a recipe with 1,500 likes read `1500` there and `1,500` on the chef card three taps away — `groupedCount` exists in `core/src/formatting.dart` precisely so counters do not disagree (B031's family). Also found by the OPT-T5 screenshot pass. | fixed | `detail_chips.dart` renders `groupedCount(count)`. |

### Review findings (Phase 26 — Discover shelves, 2026-08-22)

Found by `/code-review` over the phase's own diff, before it shipped — the same way B037–B041 were
found in Phase 23. Both are fixed in the change set that introduced them.

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B057 | 2026-08-22 | low | design_system | **`CardRail`'s numbered header lays its ranking kicker out unbounded** — the B039 class. The kicker (`RANKED BY SAVES`) was a plain non-flex `Text` in the header `Row`, and a non-flex child of a `Row` is laid out against an **unbounded** main axis, so it takes its full intrinsic width and pushes the row over instead of ellipsizing. The width gate in front of it (`width >= 700 × textScale`) decides only whether the kicker is *drawn*, which says nothing about how long that particular string is: Discover's own three fit, so no envelope test fails, but the widget is generic and the next caller's longer kicker overflows the header. Same shape as the spotlight rank pill at 3.0× (B039). | fixed | `ConstrainedBox(maxWidth: width * 0.25)` around it, so it ellipsizes like every other capped label in the header. `card_rail_test.dart` pins a deliberately over-long kicker at 700px and 2.0×. |
| B058 | 2026-08-22 | low | tooling/sql | **A comment in `drop.sql` claimed a dependency Postgres does not record.** The new `drop function if exists site_rating_prior() cascade` was annotated "`cascade` matters on this one — `recipes_popular` and `recipes_quick` cross-join it, so it has real dependents". Postgres does not track dependencies through a **quoted** SQL function body (only `BEGIN ATOMIC` bodies are parsed and recorded), so the `cascade` reaches nothing and the two callers are dropped by name two lines above. Harmless in effect, but a comment that misstates how the drop works is how the next person deletes the wrong line. | fixed | Comment corrected to say the opposite and why. |
| B059 | 2026-08-22 | low | discover | **The archive grid did not line up with the page it sits on.** Discover insets its masthead, its three shelves and their numerals by `AppSpacing.xl` (32) on a wide window, but `RecipeAsyncSliverGrid` was added as a bare sliver, so the `EVERYTHING ELSE` grid used `SliverRecipeGrid`'s own default `EdgeInsets.all(16)`. Measured off the release build at 1400px: every rule, numeral and rail card ends at x=1367 (a 32px margin) and the grid's cards end at 1379 (20px), starting 16px left of everything above them. On a page whose design is a left column and a set of rules, that reads as a misprint. Nothing overflows and no envelope test can see it — found on the first screenshot. | fixed | `RecipeAsyncSliverGrid` takes a `padding`, defaulting to the old 16 so the five full-screen surfaces are unchanged; Discover passes its own margin. `discover_screen_test.dart` asserts the first archive card and the `01` numeral share a left edge (fails at 16.0 without the fix). |
| B060 | 2026-08-22 | medium | discover | **The selected browse sort was invisible on the web.** `_SortLink` drew its underline as `Container(height: 2, color: …)` beneath the label in a `Column`. A box with no child and no width takes `constraints.smallest` when the constraints are unbounded and `constraints.biggest` when they are bounded — and the sort control is a `Wrap` that sits as a **non-flex child of a `Row`** in the wide layout (unbounded) and as a full-width child of a `Column` in the stacked one (bounded). So on the web the underline was **zero width and never drawn**, leaving nothing but a font weight to say which of Top rated / Trending / Newest was active; on a phone the same widget stretched to the full width, which forced each link onto its own line and drew a page-wide rule under the selected one. Confirmed on the release build at 1400 (no underline) and 390 (three stacked, full-width rule). The B039 family: unbounded main axis, sized by the wrong end of the constraints. | fixed | The underline is a `BorderSide` on the `Container` that holds the label, so it is the width of the text plus its padding in both directions. `discover_screen_test.dart` measures each link at 1400 and 390 and asserts the selected one carries a non-transparent bottom border; both halves fail without the fix. |

### RLS findings (BL-7 — the acceptance matrix as a signed-in user, 2026-08-23)

Found by the first complete run of [supabase/tests/rls_matrix.sql](../supabase/tests/rls_matrix.sql)
(`melos run db:rls`), the file BL-7 exists to produce. Nothing in this repo had ever exercised a
policy as `authenticated` — the same blind spot B053 sat in for months.

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B061 | 2026-08-23 | medium | schema | **Any signed-in user can like or save a recipe they cannot read, and move its public counters.** `likes_write` and `saves_write` were `for all using (user_id = auth.uid()) with check (user_id = auth.uid())` — the row is pinned to the caller, but there is **no read test at all**, so knowing a recipe's uuid is enough. `on_like_change` / `on_save_change` are `security definer` (B011), so the insert really does bump `recipes.like_count` / `save_count` on a **private** recipe. The attack is pre-inflation: seed a private recipe's counters, then the owner flips it public and the forged numbers walk straight into `recipes_trending`, `recipes_projects`' `order by save_count`, and — via `recompute_chef_stats`, which sums the owner's public recipes — the chef score and the leaderboard. Exactly the inflation route B012 closed for `recipe_views` (`anon` insert → counter) and B050 closed for a direct `PATCH` of the column; likes and saves were the third door and nobody had checked it, because `ratings_write` (`can_read_recipe`) and `views_insert` (`can_read_recipe`) both had the test and the two beside them read as if they did too. A secondary leak: `saves_select` is `user_id = auth.uid()`, so the forged save row is readable back by its author, confirming the uuid resolves to a real recipe. | fixed | `can_read_recipe(recipe_id)` added to the **`with check`** of both policies, and deliberately **not** to `using`. The split is the fix, not an accident: `with check` governs INSERT, so a new like needs read access; `using` governs SELECT/UPDATE/DELETE and stays `user_id = auth.uid()` alone, because putting the read test there would strand every liker's row the moment an owner flipped a recipe to private — and the unlike would then match 0 rows and report success (Gotcha 2), which is a worse bug than the one being fixed. Verified on the local stack: `rls_matrix.sql` is 76/76 green with the fix; restoring the old policy turns exactly **D18** (`like an unreadable recipe must FAIL`) and **D20** (`save an unreadable recipe must FAIL`) red, and **D19** (`unlike an unreadable recipe still works`) stays green in both, which is what proves the `using` half was left open on purpose. `C16`/`C17` cover the other direction — a shared-with user can still like and save the private recipe they were given. |

### Recipe detail v2 bugs (found by the new text-scale envelope test, 2026-08-23)

Three `RenderFlex` overflows, all caught by `apps/app/test/recipe_detail_v2_test.dart`'s
`layout envelope` group — which pumps the whole page at {1000, 1440} px × {1.0, 2.0} text scale and
asserts no exception — on its **first run**, before the layout was ever built to the browser at that
scale. None of them shipped. Logged because they are the fourth, fifth and sixth instance of the
family B001/B002/B016/B026/B038/B039 live in, and because the pattern is now unambiguous: **a
non-flex child of a `Row` takes its intrinsic width, so no amount of flexibility in its siblings
prevents an overflow** (Gotcha 21). Every one of the three was a control whose *label* grows with
text scale sitting next to something flexible.

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B062 | 2026-08-23 | medium | recipe_detail | **The ingredients rail's servings row overflowed by 50px at 2.0× text scale.** `Row(Expanded('Servings'), IconButton, SizedBox(48), IconButton)` inside the fixed 352px rail. The two `IconButton.filledTonal`s and the count are non-flex and M3 scales an icon button's padding with the text scale, so the three of them together exceeded the rail's 318px content box on their own — the `Expanded` label shrank to zero and the row still overflowed. | fixed (pre-ship) | The row is a `Wrap` with `WrapAlignment.spaceBetween`: one line at 1.0×, and at 2.0× the stepper drops to its own line under the label. |
| B063 | 2026-08-23 | medium | recipe_detail | **The rail's footer row overflowed by 43px at 2.0×.** `Row(TextButton('Clear checks'), SizedBox, Expanded(note))` — same shape as B062, and `Clear checks` alone is wider than the rail at that scale. | fixed (pre-ship) | Same fix: a `Wrap`, so the note wraps under the button instead of the row overflowing. |
| B064 | 2026-08-23 | medium | recipe_detail | **The cook-mode teaser overflowed by 52px at 1000px × 2.0×.** `Row(icon, Expanded(copy), FilledButton('Start cooking'))`. At 2.0× the button is ~390px — wider than the whole 393px method column at the narrowest expanded window — so it overflowed there while passing at 1440px. The one that proves testing a single width is not enough: the two-axis matrix is what caught it. | fixed (pre-ship) | The teaser stacks above `1.3×` text scale (`_CookModeTeaser._kTeaserStackScale`) — button on its own line under the copy — the same threshold-not-measurement shape `/chefs` uses to drop to one column (Gotcha 22). The rail width and quantity gutter are now bounded against text scale too (`kDetailRailMaxScale`), rather than fixed. |

### Review findings on the Phase 27 v2 layout (2026-08-23)

Both found by `/code-review` on the v2 detail change set, after the three overflows above were
fixed. Neither is a layout bug and neither is visible on this machine's fixtures — which is the
point of logging them: one is invisible because every seeded snapshot is empty, the other because
no seeded ingredient reaches the state.

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B065 | 2026-08-23 | high | core/perf | **Opening a recipe downloads every version's full content snapshot to render one label.** The v2 header band watches `recipeVersionsProvider`, and `SupabaseRecipeRepository.versions()` was a bare `.select()` — so it asked for `recipe_versions.content_snapshot`, a whole recipe serialized as `jsonb` (~10 KB a version, nine versions on some recipes), on **every** page open. Nothing on the client reads that column: the header band renders the count and the history sheet renders `version_number` / `change_summary` / `created_at`. Same class as OPT-P1's `search_tsv` (`select *` shipping a column nobody reads), but worse, because the payload grows without bound as a recipe is edited. Undetectable locally: `seed.sql`, `seed_recipes.sql` and `2_sim_generate.sql` all write `'{}'::jsonb`, so the fixtures make a ~90 KB read look like a 900-byte one, and only a recipe edited through `save_recipe` (which snapshots real content) shows the true size. | fixed | New `kRecipeVersionSelect` in [recipe_queries.dart](../packages/core/lib/src/repositories/recipe_queries.dart) lists the seven columns the client actually reads and omits `content_snapshot`; `versions()` passes it. The model's `@Default(<String, dynamic>{})` covers the now-absent key, so nothing else changes. A "diff two versions" / "restore" feature would need the column and must take **its own** read for one version, not widen this list. `recipe_repository_test.dart` asserts the select excludes `content_snapshot`, keeps `version_number` and `change_summary`, and still decodes. |
| B066 | 2026-08-23 | medium | recipe_detail | **An ingredient with a unit and no quantity rendered `—` and dropped the unit.** `_IngredientRow._quantityLabel` was `if (quantity == null) return note ?? '—'`, so `unit: 'cup', quantity: null` showed a dash and the unit vanished from the rail entirely — the v1 renderer (`recipe_content_views.dart:61-73`) appends the unit whether or not there is a number, so the same row read differently either side of the 1000px branch. Reachable without any bad data: `quantity` is `numeric null`-able and the editor's field parses a decimal, so typing `1/2` fails the parse and saves the unit alone. No seeded ingredient is in that state, so no fixture shows it. | fixed | The gutter's chain is now `quantity + unit` → `unit` → `note` → `—`, with the unit ahead of the note (a unit with no number is a data defect worth seeing) and the note riding beside the name whenever it did not take the gutter, so a `unit + note + no quantity` row keeps both halves rather than trading one for the other. `recipe_detail_v2_test.dart` pins all four states in one pumped recipe and asserts the note-as-quantity case does **not** also print beside the name; three of its assertions fail against the old code. The fake repository now takes the recipe so a test can vary the fixture. |

### Cook mode, pre-ship (2026-08-23)

All three were caught by `apps/app/test/cook_mode_test.dart` on its first run, before cook mode was
built to a browser. None shipped. **B067 is the one worth reading** — it is a new mechanism, not
another instance of Gotcha 21, and it is now Gotcha 25.

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B067 | 2026-08-23 | medium | recipe_detail | **A `LayoutBuilder` used to cap a non-flex `Row` child measures `infinity`, so the cap caps nothing.** Cook mode's web top bar is `Row(IconButton, Expanded(title), chips)`, and the chips were capped the documented way — non-flex inside a `ConstrainedBox` at `constraints.maxWidth / 3` (the B038 shape) — with the `LayoutBuilder` supplying that width placed *inside* the Row. But a non-flex child of a `Row` is laid out with an **unbounded** main axis (Gotcha 21), so the builder's `constraints.maxWidth` is `double.infinity`, the cap is `infinity / 3`, and the chips took their full intrinsic width exactly as if the `ConstrainedBox` were not there. Overflowed by 186px at **1000px × 2.0×** and was green at 1440 × 2.0 and at 1000 × 1.0 — the fix for a Gotcha 21 overflow reproducing the Gotcha 21 overflow, while *looking* like the accepted shape in review. | fixed (pre-ship) | The `LayoutBuilder` moved **outside** the `Row`, where the constraints come from the bar's own box and are real. The rule generalises: a `LayoutBuilder` reports the constraints of *its own position*, so measuring inside an unbounded position measures nothing — hoist it to the nearest bounded ancestor. Now Gotcha 25. The two-axis envelope is what caught it; a single width or a single scale would have passed. |
| B068 | 2026-08-23 | low | recipe_detail | **`MetaChip` could not degrade.** Its `Row` held a bare `Text` with no `maxLines`/`overflow` and no flexible sibling. Harmless for the short facts it was written for (`12 min`, `350°F`), but cook mode puts a whole ingredient in one (`1.25 cup Unbleached wheat flour`), which is wider than a 390px phone — so the pill overflowed by 15px rather than shrinking. The review checklist's §7 predicate exactly ("a `Text` inside a `Row` with no `maxLines` + `overflow`"), sitting in a shared widget since Phase 3 and only reached once a caller gave it a long label. | fixed (pre-ship) | The label is `Flexible` with `maxLines: 2, overflow: ellipsis` — two lines rather than one so a long ingredient wraps inside the pill instead of ellipsising the word that says which ingredient it is. Every existing caller passes a short label and is unchanged. |
| B069 | 2026-08-23 | low | recipe_detail | **"Not done — back to the last step" did nothing on the finish screen.** `CookSessionNotifier.goTo` opened with `if (index == state.stepIndex) return;`, and the finish screen is a *flag* on the session rather than a separate index — so the control, which targets the step the cook is already standing on, matched the guard and returned before clearing `finished`. The finish screen stayed up and the tap read as broken. A guard on one half of a two-part state. | fixed (pre-ship) | Guard is now `if (index == state.stepIndex && !state.finished) return;` — the early return only fires when *both* halves are already where they are being asked to go. Its own test in `cook_mode_test.dart`. |

| B070 | 2026-08-23 | low | recipe_detail | **The ingredients rail's heading row overflowed by 9.5px at 390px × 2.0× — in a widget that had been green for a week.** `Row(Expanded('Ingredients'), Text('n of m gathered'))`: the counter is non-flex and therefore laid out unbounded (Gotcha 21), but it *fitted* everywhere the rail had ever been pumped, because the expanded page gives the rail a `352 × 1.4` = 493px column. Compact v2 reused the same widget at the 358px content box of a 390px phone and the same code overflowed. B062/B063 fixed the two rows *below* this one on exactly this diagnosis and left the heading, since nothing then rendered it narrow enough to fail. The lesson is about **reuse**, not about the row: a widget's envelope is the set of widths it has actually been pumped at, and giving an existing widget a new caller re-opens it. | fixed (pre-ship) | Same `Wrap` with `WrapAlignment.spaceBetween` as the servings row and the footer, so the counter drops under the heading at 2.0×. `recipe_detail_test.dart`'s new envelope group (390 / 600 / 800 × {1.0, 2.0}) is what caught it and is what keeps it caught. |

### Phase 28 — nutrition facts (2026-08-24)

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B071 | 2026-08-24 | medium | core | **`Recipe.toJson()` emitted a `RecipeNutrition` object instead of a map.** `explicitToJson` is off for `packages/core`, so `json_serializable` writes `'nutrition': instance.nutrition` for a nested model — a value `jsonEncode` cannot serialize. It had never come up because every other nested field on `Recipe` (`ingredientGroups`, `stepGroups`, `owner`) is `includeToJson: false`, so `nutrition` was the first one that had to flatten itself. No production caller was affected — `_writablePayload` builds its map by hand and already called `recipe.nutrition?.toJson()` — so the only symptom was a test asserting `r.toJson()['nutrition']` failing with `expected a map`. Left alone it is a landmine for the next `jsonEncode(recipe.toJson())`. | fixed (pre-ship) | `@JsonKey(toJson: _nutritionToJson)` on the field, with a top-level `Map<String, dynamic>? _nutritionToJson(RecipeNutrition?)` beside the `part` directives. Scoped to the one field rather than turning `explicitToJson` on for the package, so no other model's output shape moves. Pinned by `packages/core/test/nutrition_test.dart`. **The general rule: a new nested-model field on any `core` model needs an explicit `toJson:` unless it is `includeToJson: false`.** |
| B072 | 2026-08-24 | low | recipe_editor | **A collapsed nutrition panel would have swallowed an invalid entry.** The panel is collapsed by default; the first draft removed its `TextFormField`s from the tree when collapsed, and a `FormField` that leaves the tree also leaves `Form.validate()`. So typing `1/2` into Calories, collapsing the panel, and saving would have passed validation and then been dropped by `double.tryParse` — the field silently saved as "no calories". Same shape as B066: a parse that fails quietly on a number a user typed. Caught in review of the panel's own design, before it was pumped. | fixed (pre-ship) | `Visibility(visible: expanded, maintainState: true)` keeps the fields registered with the `Form` while collapsed, so an invalid entry still blocks the save; `_save()` then re-opens the panel — **only when `EditNutrition.hasInvalidEntry`**, so a blank Title does not unfold eleven boxes that have nothing to do with it. That moved `expanded` out of the panel's own state into `_RecipeEditorScreenState`. Three tests in `recipe_editor_test.dart`, and the collapsed one is the load-bearing one: it goes red when `maintainState: true` is removed, which the expanded-panel test does not (verified by removing the flag and re-running). |
| B073 | 2026-08-24 | low | recipe_detail | **The extracted servings row claimed a scaling the ingredient list did not perform.** Hoisting the stepper out of `IngredientRail` into `ServingsRow` (Phase 28) restated its "is this scaled?" test as `servings != recipe.servings`, dropping the original's `recipe.servings == 0 ? 1.0 : …` guard. The two then disagree on exactly one input: a recipe stored with `servings = 0` — reachable, since the editor's box is `int.tryParse(…) ?? 1`, `save_recipe` only coalesces a *null*, and the column has no positive check. Tapping **+** on such a recipe printed *"Scaled from 0 — quantities in colour changed"* while the rail's own guard kept every quantity uncoloured and unscaled. Nothing overflows, nothing throws; the banner just describes a change that did not happen. Found in review of the diff, which is the general lesson: an extraction that restates a predicate instead of moving it is where a "pure refactor" stops being one. | fixed (pre-ship) | `ServingsRow` mirrors the guard — `recipe.servings != 0 && servings != recipe.servings` — with a comment naming why it must stay in step with `IngredientRail`'s `factor`. Test: `recipe_detail_test.dart` — "a 0-serving recipe never claims to be scaled", which also asserts the quantity is still unscaled, since that is what makes the absent banner correct. |

### Phase 29 — auto nutrition (2026-08-25)

| ID   | Date       | Severity | Area | Description | Status | Fix / Commit |
| ---- | ---------- | -------- | ---- | ----------- | ------ | ------------ |
| B074 | 2026-08-25 | high | tooling/db | **The documented B033 psql pattern corrupts every multibyte character it loads.** `Get-Content file.sql -Raw \| docker exec -i … psql -f -` re-encodes on the way through PowerShell 5.1: the UTF-8 file (no BOM) is decoded as ANSI and the pipeline re-encodes to the console codepage, so `ñ` (C3 B1) reaches psql as `Ã±` (C3 83 C2 B1) and is **stored** that way — `Jalapeño` in the database at `length() = 9, octet_length() = 11` instead of 8/9. Every accented literal in every file ever loaded through that pipe is affected (`seed_recipes.sql`'s `jalapeño` and `Gruyère`, the Phase 29 registry aliases), and it is invisible in normal use because psql renders the bytes back the same way and every *assertion* in the repo is ASCII. Found by Phase 29a's own verification — the accented-alias exact-match probe (`select … where alias in ('jalapeño','gruyère')`) came back empty. | fixed | Byte-faithful form is **cmd.exe redirection**, which never decodes: `cmd /c "docker exec -i supabase_db_secret-sauce psql … -f - < file.sql"`. CLAUDE.md's B033 block now shows that form. Local database rebuilt clean and re-verified with `length()`/`octet_length()` probes. **Open half, for the owner:** if the hosted project was ever loaded through the PowerShell pipe, its accented rows carry the same mojibake — probe with `select title from recipes where octet_length(title) - length(title) not in (0, …)`-style checks before trusting hosted content, and re-seed the affected rows if found. |

## Template for new entries

```
| B001 | 2026-07-28 | high | recipe_editor | Saving edit does not bump version_number | open | |
```
